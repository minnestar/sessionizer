<%= semantic_form_for([:admin, :legacy, @event]) do |f| %>
  <%= f.semantic_errors %>

  <%= f.inputs %>
  <%= f.actions do %>
    <%= f.button 'Create Event', :class => 'button'  %>
  <% end %>
<% end %>
