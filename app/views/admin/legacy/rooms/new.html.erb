<%= semantic_form_for([:admin, :legacy, @event, @room]) do |f| %>
  <%= f.semantic_errors %>

  <%= f.inputs %>
  <%= f.actions do %>
    <%= f.button 'Add Room', :class => 'button'  %>
  <% end %>
<% end %>
