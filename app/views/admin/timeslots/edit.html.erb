<%= semantic_form_for([:admin, @timeslot]) do |f| %>
  <%= f.semantic_errors %>

  <%= f.inputs %>
  <%= f.actions do %>
    <%= f.button 'Update Timeslot', :class => 'button'  %>
  <% end %>
<% end %>

