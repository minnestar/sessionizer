<% semantic_form_for(@session) do |f| %>
  <%= f.inputs :title, :description %>

  <%= f.inputs :name, :email %>

  <% f.inputs do %>
    <%= f.input :categories, :as => :check_boxes %>
  <% end %>

  <%= f.buttons %>
<% end %>
